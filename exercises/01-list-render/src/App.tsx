import { RESUMES } from "./data";

/**
 * TODO
 * Render one <Card /> per resume in RESUMES.
 * Each card shows the name, the role, and the score.
 */
export default function App() {
  return (
    <main className="wrap">
      <h1>Resumes</h1>
      <p className="hint">Nothing here yet — that is your job.</p>
    </main>
  );
}
